# Cookieless-аналитика (Plausible).
# Задай свой домен статистики — тогда скрипт аналитики подключится на странице.
# Если переменная не задана, аналитика НЕ загружается вовсе.
# Пример: PUBLIC_PLAUSIBLE_DOMAIN=stats.noxchiin-mott.example.com
PUBLIC_PLAUSIBLE_DOMAIN=
